POSTGRES_DB=database
POSTGRES_USER=user
POSTGRES_PASSWORD=password

# Хост — имя сервиса compose. Для запуска вне docker переопределяется
# в bot/dev.env, members_sync/dev.env и packages/db/dev.env.
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
